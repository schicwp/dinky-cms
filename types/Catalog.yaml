name: Catalog
workflow: ConceptWorkflow
nameField: title
fields:
  - name: title
    type: String

  - name: code
    type: String
    regex: "[A-Za-z]+"
    index: true

  - name: description
    type: String

  - name: broader
    type: Collection
    collectionType:
      type: String
      regex: "[A-Za-z]+"