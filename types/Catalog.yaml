name: Catalog
workflow: ConceptWorkflow
nameField: title
fields:
  - name: title
    type: String

  - name: alias
    type: String
    regex: "[A-Za-z]+"
    index: true

  - name: description
    type: String
